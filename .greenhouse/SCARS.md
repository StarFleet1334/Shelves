# SCARS

One line per thing that broke here and what got past it.
Every tending is handed the tail of this file before it starts, so
the same wall is not walked into twice.

- `node tests/harness.js 9` printed "all 0 scenarios passed" and exited 0 -> a number that names no scenario now FAILS, or a roadmap proof would tick itself before the test existed
- rung 1 returns for EVERY repo the moment ONE row carries chips -> the ladder must treat page chips as a floor and keep climbing for the rest (measured: 4 repos, 1 chipped, 3 shelved as Ungrouped with zero requests)
- a repo page that 429s or 404s is swallowed and the repo lands in Ungrouped with no warning, while the toolbar still says `via api (public)` -> count the unread and say so (P.IV)
- a 401 on `/user/repos` never retries the unauthenticated endpoint, yet the source line already says `api (public)` -> either fall back for real or stop claiming it
- topics scraped from a repo page must be scoped to `.Layout-sidebar`, or a README full of /topics/ links lies about the repo's tags
- a row's own textContent is read AFTER the note margin is appended -> every row matched the query "note" and a saved note counted twice; capture `li.dataset.shText` once, before anything is added to the row
- the filter forces shelves open to show hits, and the `toggle` listener wrote that to the collapse store -> a cleared search left every closed shelf hanging open; park the real state in `data-sh-was-open` and stand the listener down while `data-filtering` is set
- GitHub's <meta name="description"> is "<about>. Contribute to o/n development…" and there is no way to tell its joining period from one the owner typed -> strip it with the boilerplate; a stray "." on every description is the more visible wrong
- a proof that names a harness scenario by NUMBER ticks itself the moment a scenario is inserted above it (appending facts/find/note made `harness.js 8` mean the facts scenario, which passes) -> proofs name scenarios by keyword, and a selector matching nothing fails
- `.sh-margin { flex: 0 0 100% }` appended to GitHub's repo row: `d-flex` DOES NOT WRAP, so a 100% basis with flex-shrink 0 does not drop to its own line — it takes the width and refuses to give it back, collapsing the description from 599px/2 lines to 94px/13, breaking words mid-word -> put the note INSIDE the row's text column (the li child holding the h3), and give any li-level fallback `flex: 1 1 100%; min-width: 0`. jsdom cannot see this; tests/row-layout.html + aether/look.py can
- a shelf glyph the font stack lacks renders as the MISSING-GLYPH BOX, which at 10px looks enough like a hollow square marker to survive being looked at: `□` (U+25A1) and `●` (U+25CF) are both tofu in GitHub's own stack on Windows, and `●` was slot 0 -> measure every candidate against the width of U+FFFF (guaranteed to have no glyph anywhere); equal width IS the box. tests/glyph-probe.html
- a fixture that draws only the shelves this month happens to have draws only some of the palette: the first identity shot rendered 9 of 12 slots and the tofu was sitting in one of the 3 it never reached -> the fixture names twelve labels, which the collision walk guarantees exhausts every slot
- ONE lightness cannot serve two themes: at hsl(H, 62%, 52%) blue measured 3.3:1 on #0d1117 while yellow measured 2.2:1 on #ffffff, because luminance is not a function of hue -> solve each hue for its own lightness at a common relative luminance (0.19), which clears 4.3:1 on both. This extension has no theme detection on purpose, so the colour has to work on both backgrounds rather than pick one
- reporting a topic family's reach as the SUM of its spellings inflates it: `ai-project` (2) + `aiproject` (1) is three repos, not four, when one repo wears both -> count the union of repo indices, or a labelling problem reads as a bigger collection
- a gap counted over every repo reports the SOURCE's shape as the reader's failing: api.github.com's body carries no README, so "no README" over API-answered records is every repo on a token-holding account -> every finding declares the field it needs, and a repo whose source could not answer leaves the numerator AND the denominator (P.XIII). What could not be asked is printed, because a silently smaller denominator is its own lie
- one string served as both the button's word and the name of the set it selects: the button says SHOW, so the bar afterwards said "2 show of 5" instead of "2 no README of 5". Read fine in the code, nonsense on the page
- the repo-page chip lost its glyph for the leftovers shelf: the glyph was inside the branch that paints the colour, and the plain shelf has no colour BY DESIGN -> `Ungrouped` wore its dot on the shelf and nothing on the chip. Two drawings of one identity that disagree is the failure the whole system exists to prevent. Caught only in a real browser
- the background top-up was designed as a service worker + the `alarms` permission, which is the obvious shape and the wrong one: a worker cannot ride the session cookie, so it could only have warmed what the API already answers in ONE call — the expensive half is the private repos, and only a same-origin fetch from a github.com page can read those. Building it dropped a permission instead of adding one (P.VII)
- writing `shelfMap` from render() without adding it to main.js's QUIET list is a render -> storage change -> reload -> render loop
- a canary with no sample floor is a canary that gets turned off: below five pages read, a run of genuinely sparse repos is indistinguishable from a dead selector. And the fixture for it must NOT be an empty page — the failure worth catching is the one where the parse still returns something that looks right
- the repo-page chip staked the whole feature on two CLASS NAMES (`.Layout-sidebar`, `[data-testid=repository-sidebar]`) and neither matched the real logged-in page: no chip at all, beside an About panel plainly on screen. A class is the half of GitHub's markup that churns -> the class is a fast path and the finder is structural, climbing from the "About" heading to the first ancestor holding another sidebar landmark, bounded by "never an ancestor containing the <h1>" and "never wider than half the window"
- rendering nothing was the right ANSWER and the wrong silence: "GitHub renamed the sidebar", "the loaded extension is a stale build" and "this is not a repo page" all looked identical, and telling them apart cost a round trip through a human pasting DOM into a console -> mark.js now console.debug()s which of them it is, and says so again when it lands via a fallback
- CHARTER measurement 1 ("the profile list renders no topic chips") went stale under us: GitHub now renders them, so rung 1 answers and short-circuits. 77 repos resolved `via page` with 9 tagged and 68 silently Ungrouped for zero requests. A measurement is true on a date, not forever — the ones that "determine the entire architecture" are the ones to re-check
- nothing anywhere asked WHOSE profile was open: `isRepoTab()` tests the URL shape and the tab param, so a stranger's Repositories tab sent the reader's Bearer token to `/user/repos` (which answers with the READER's repos — useless on that page) and then scraped every one of the stranger's repo pages with the reader's session cookie. Measured: 12 repos, 12 authenticated fetches, 12 permanent cache entries; at 600 repos that is 600 requests for one click on a link -> the free rungs work anywhere, the expensive ones only on your own profile (P.XIV). "Cannot tell who is signed in" must count as YOURS, or a moved meta tag disables the extension for everybody at once
- `scrape()` — the highest-volume path in the extension, hundreds of authenticated same-origin fetches — was the ONLY one with no backoff, while warm.js, which makes six, had one. Measured against a server answering 429 to everything: 40 of 40 requests issued, and a page of Ungrouped repos with no explanation at all. The asymmetry was exactly backwards
- there was no cache eviction path ANYWHERE: an entry written once lived forever, and warm.js refreshes everything it finds, so a single visit to a stranger's 300-repo profile left the browser re-fetching somebody else's repositories for as long as the extension was installed. Prune generously (four TTLs, floor 90 days) or the top-up and the pruner fight each other
- two path segments went from a page-supplied href straight into `fetch("/" + name)`. The leading slash contains every crafted form to github.com — measured, no SSRF — but not WHICH github.com path: `/settings/tokens/x` resolved to an authenticated GET of the reader's token page, whose text was then cached and made searchable in their own UI. A contained capability is still a capability nobody asked for
- fixing a quadratic exposed an unbounded DOM behind it: with the pairwise scan gone, 9 000 topics resolved in 45 ms and returned 7 741 findings — 7 741 buttons with 7 741 listeners. An unbounded computation and an unbounded render are the same bug in different clothes, and the second one only becomes visible once you fix the first
- CHARTER said "never stores your repositories anywhere". True when the cache held topics; false from `facts.js` onward, which caches descriptions, README openings and private repo NAMES. A promise the code contradicts is worse than no promise — and the sentence most likely to go stale is the one written when the claim was easy to keep
- rows on the leftovers shelf stood tall with an empty band under two lines of text, and the first diagnosis was a GUESS DRESSED AS A MEASUREMENT: GitHub's sparkline arriving with a degenerate viewBox, "reproduced" at 87px -> 214px in a fixture written from that same guess, and fixed with `#shelves-host li svg { max-height: 32px }`. None of it was true of the page. The real fragment is `<svg width="155" height="30" aria-hidden="true">` — sized attributes, NO viewBox — and taking the ceiling back off the LIVE page changed the height of 0 of 54 rows. A fixture built from the hypothesis can only ever confirm it; the fixture has to be built from the page
- what it actually was: `.sh-margin[data-has="0"]`, the invisible pencil reserved on every note-less row. Measured on a real 54-repo profile — GitHub's own rows 109px, ours 131px, and switching off the empty marker put all 54 back to 109px exactly. 22px x 54 rows is 1 188px of blank. It reads as a bug ONLY on the leftovers shelf because a repo lands there for having no topics and usually has no description either, so 22px of nothing sits under a two-line row and the empty half is as tall as the full half; on a shelf whose rows carry a description and chips it is a tenth of the row
- and the reason it was reserved was right — a pencil appearing on hover grows the row and shoves every row below it under the cursor. The answer is not to make it conditional but to take it OUT OF FLOW: parked at `top: 100%` in the 24px of bottom padding the row already has. Scoped by `#shelves-host` (the repo-page chip has no row to park in), by `.sh-col` (the unrecognised-markup fallback appends to the <li>, where `top: 100%` is the next row) and by `[data-has="0"]` (a written note is content and belongs in the flow)
- a size complaint about a re-parented row still deserves the ancestry question first — moving their <li> from `#user-repositories-list > ul > li` to `... > #shelves-host > details > ul > li` does silently drop any rule of theirs written with a child combinator. It just was not the answer this time, and the way to tell is to strike our own rule out on the live page and see whether anything moves
- `flex-wrap: wrap` for the unrecognised-markup fallback was applied to EVERY row in the host, rewriting GitHub's own flex layout on the many rows whose margin sat safely in a text column -> scoped behind a `data-sh-loose` flag set only when the fallback actually fires
- writing `
` inside a bash heredoc to build a JS string literal collapses it to a REAL newline and breaks the file — twice now, in two different files, both times silently until node parsed it. Build JS through the Write tool or a script file, never through `python - << EOF` with escapes in it

- and the row-height bug the owner was actually reporting was NONE OF THAT, because it is invisible signed out and both sessions measured signed out: `fetchRestOfPages` imports rows from page 2..N, and GitHub's commit sparkline is a `<poll-include-fragment>` carrying a `data-nonce` from the response it was served in. Imported into this document it never reaches the network at all (measured on the live signed-in page: 30 requests for GitHub's own 30 rows, ZERO for the 47 merged) -> straight to `is-error`, which reveals the `data-show-on-forbidden-error` blankslate GitHub ships inside it. That blankslate is a full-width page element landing in a 145px column, so "Uh oh! There was an error while loading." wraps to about one character per line: 47 rows at 1 416px each, 70 431px of page against 9 171px once the fragments are dropped. It reads as BLANK because at that width nothing in it is legible
- the lesson under it: a logged-out profile is a DIFFERENT PAGE, not a cheaper sample of the same one. Private repos, star buttons and — the one that mattered — nonce-checked fragments only exist in the signed-in response. Two rounds of "measured in a real browser" were measured on a page the owner never looks at
- and an unpacked extension does NOT pick up an edited content script on a page reload; Chrome caches it until the extension itself is reloaded at chrome://extensions. A fix verified by injecting it into the live page is a verified fix; the same fix "verified" by reloading the tab is a measurement of the old build
- the find box searched the half of the row nobody can see. `li.textContent` takes GitHub's star control with it — a confirmation dialog, an "add this repository to a list" menu and an empty-state panel, all ordinary hidden DOM. MEASURED on the live signed-in page: 322 characters captured per row against 53 on screen, and `star`, `starred` and `list` each matched 77 OF 77 ROWS while `sorry` matched 30. Not obviously broken, just quietly useless -> read `column(li)` instead, which the note marker already computes: it is the prose half of the row and the star column is simply the other child. After: star 1, list 0, starred 0, sorry 0, and java/gym/typescript/wiremock unchanged. Signed out there are no star buttons and none of this exists
- rung 1 was a SHORT CIRCUIT wearing a floor's name for a whole release: `if (answered() > 0) return` ended the ladder for the entire collection the moment ONE row carried chips. Written when the profile list rendered none, so it could only ever fire when every row had them; GitHub started rendering them on some rows and the same line changed meaning underneath it. Measured live: 9 of 77 chipped -> `via page`, 68 in Ungrouped, zero requests, `warning: ""`, reading exactly like success. And it did not only cost grouping — the record it returns has three fields, so find lost descriptions/READMEs/languages/licences, the audit's whole repositories half went to "not asked", and the canary CANNOT FIRE from that path at all (`health` is "" there), so the run that would notice a moved sidebar was the run that never happened
- fixing the gate is half the fix: `facts = names.map(n => byName.get(n) || {name, topics: []})` overwrites a chip answer with an empty record whenever the API cannot see that repo, which hands rung 4 a bill for repos already answered for free. A floor that costs a request is not a floor
- and the source line had to become a LIST. One rung name was enough while a run could only be one rung; with chips as a floor a render is routinely two or three, and `via page` alone hides the requests that answered most of the collection — P.IV pointing the wrong way. Cost of the whole change on the live profile: ONE api call, zero page reads, because the public endpoint settles "this repo genuinely has no topics" for free and only what it cannot see reaches rung 4
- the 401 branch set `source = "api (public)"` and never asked the public endpoint. So the sentence the product stakes its trust on named a rung that had not run, AND `byName` stayed empty so every repo fell through as missing: one expired token turned a one-request page into a page that reads every repository you own, one at a time. The fixture could not even express the bug — one `apiRepos` answered both doors — so `apiPublic` had to exist before the test could
- the highest-volume path had a backoff for "GitHub says stop" and nothing at all for "do not start". `scrapeMax` is the ceiling and the `read N more` button is the other half — a cap with no way past it is not a choice, it is a smaller silence. Deferred is NOT counted as unread (unread means we tried and failed; its cure is rescan) and NOT drawn as a warning (nothing went wrong; filing a deliberate limit beside "token rejected" teaches the reader to read a choice as a fault)
- scenario 1 asserted the bug: three chipped rows of four, then `assert(counters.api === 0)`. A fixture written from the old behaviour will defend it — the scenario now gives chips to EVERY row, which is the only shape in which "the page alone answered" is true
- accepting a suggestion from a cold start DELETED every shelf already on the page: with `settings.groups` empty the shelves are auto-derived from topics, and writing the FIRST group flips the page into configured mode, where anything matching no group falls to leftovers. Measured on the live profile — 54 repos, 1 tagged — one press of `add wiremock (3)` and the `config` shelf was simply gone -> the accept carries the shelves already on screen into the same write, in the order they are drawn, with the new one last. The first write to a setting that switches a MODE is never only that setting
- and the offer beside it counted repos it could never receive: after accepting `wiremock`, the next suggestion read `add Java (26)` while three of those 26 were pinned to `wiremock`, and an override outranks every topic, so a Java shelf could never have got them -> never count a repo that already has a shelf, which settles a second case for free: with no groups configured every topic is ALREADY an auto-derived shelf, so offering to add one was offering the reader a shelf they were looking at
- `identity()` walked ONE slot for both channels, so at thirteen shelves the loop ran the full twelve, returned to where it started and handed out a duplicate hue AND glyph together — both channels failing at once, which is the single thing a second channel exists to prevent, while the README stated the opposite as a fact ("they will still have different glyphs — the shape is the second channel and it never agrees with the wrong hue"). Proved with sixteen labels: three shelves on slot 11, all `▽`, all one colour. Not a corner case — auto-grouping makes one shelf per distinct topic and `suggest` makes a thirteenth one click -> hue and glyph walked independently, twelve or fewer unchanged (twelve distinct hues AND twelve distinct glyphs) and the PAIR carrying the identity above that, 144 of them. Walking the pair space on its own is NOT enough: it spends the palette badly, twelve shelves coming out in eight hues while four sat unused. The honest cost is now written down instead of denied — past twelve, one channel necessarily repeats and the other is what tells two shelves apart
- the first-day verbs wrote the READER'S OWN CONFIGURATION from a stranger's page. P.XIV narrowed the expensive rungs to your own repositories; nothing narrowed the writes. Measured on a real stranger's profile: one press offered `add subsurface (3)`, wrote `settings.groups` over SYNC naming somebody else's topics, pinned overrides keyed to their repos — and because the first entry in `groups` turns auto-grouping OFF, the reader's own profile then drew `Ungrouped: 3`, every shelf they had gone. The read-only half is fine on a stranger's page and stays; only the verbs that write your setup stand down, and they stand down by not being handed to render() at all, so there is no affordance to press
- the repo-page chip called `bucketFor(topics, settings)` and never learned about overrides — so a repo pinned to `keep` wore `elsewhere`, and an untagged one pinned to a real shelf wore `Ungrouped`, uncoloured. The feature disagreeing with itself on the one page built to agree with it, which is the scar the shelf map exists for, walked into from the other side. And `labelsFor()` preferred `settings.groups` over the stored map, so once a shelf could exist because the reader PUT a repo there the two pages resolved identity over different label sets: `rag` drawn ◉ on the shelves and ◇ on its own repo page
- `Math.min(bench.at(owner), untagged.length)` is the one index past the end, and the untagged list SHRINKS every time the reader tags something — which is the feature working. Walk to 3 of 5, tag three repos, come back to a list of 2: the button opened nothing, read "3 of 2", and stayed dead for good because nothing ever moved the bookmark back. The comment beside the store claimed the opposite in as many words. Wrap with `%`, never clamp
- "put this on the leftovers shelf" and "forget my opinion" are two verbs sharing one gesture, and they only agree for a repo with no topics. Deleting the override on any move to leftovers was a silent no-op for a TAGGED repo: row moved, counts changed, storage kept nothing, next load put it back. The question is what the repo would do with NO opinion — if it would land there anyway the key is noise, otherwise the reader has said something
- `.sh-shelf { overflow: hidden }` (there for the rounded corners) clips anything absolutely positioned inside it. A shelf-pick menu opened on one of a shelf's last rows overhung by 26px and `elementFromPoint` on its bottom entries returned the NEXT shelf's summary — unclickable. Same container, same trap as a sticky shelf header. Lift the clip only while a menu is open, and only on the shelf holding it
- and two that were only ever going to be caught by a machine: a python patch wrote a raw NUL byte into view.js (`"\0glyph"` where `"::glyph"` was meant) — it parses, it runs, and it silently decides the glyph channel, while GNU diff calls the file binary; and `moveRow` counted `querySelectorAll("li")`, which includes a row's own nested lists, so one move made a 2-row shelf report 4
- `/` was never ours. GitHub binds it on `document` too, and their script runs long before a content script at `document_idle` — so their listener is registered first, fires first, and opens the global quick-search overlay; `preventDefault` from a later listener is far too late. MEASURED only in a real browser, and only because the NEXT step of the probe failed: a Playwright click on the Language dropdown was intercepted by `__primerPortalRoot__`, which is GitHub's search overlay that our own `/` had just opened. Register the map in the CAPTURE phase and `stopPropagation` in every branch that acts — capturing without stopping leaves their handler to run anyway
- a `toggle` on a `<details>` is a QUEUED TASK, not a synchronous dispatch, so setting `d.open` before attaching the listener does not save you — a listener attached a hundred lines later still receives it. Every render was therefore writing an open-state key for every shelf it drew open, and a progressive first frame would have persisted names the finished page never draws again, into a store (`shelves:open:<owner>`) with no eviction path
- the progressive render's real hazard was never the rendering: it was that row-level listeners are attached ONCE and outlive the pass that made them. `const { topics, facts } = await S.resolve(…)` leaves both names in their dead zone for the whole of rung 4 — which is exactly the window in which the reader now has a usable page to click on — so a drag or a note saved during it would have thrown a ReferenceError rather than merely being stale. Handlers read `curTopics`/`curFacts`, assigned by both passes
- and it re-buckets rather than re-rendering, because swapping a second host in is four lines and silently loses: the find box's text, the filter that text applied (the `sh-hide` classes ride on the ROWS and would survive with nothing on screen explaining them), `data-sh-was-open`, the open audit panel, keyboard focus, an open shelf-pick menu and the `data-menu` flag that stops its shelf clipping it — and re-parents every row a second time, which is a second chance to cut off GitHub's lazy fragments mid-flight
- GitHub's own "Find a repository" box is SERVER-SIDE (`data-throttled-autosubmit` → a fetch), and the `data-filterable-for` / `data-filterable-type` attributes on its `<ul>` are inert — measured: typing hides no row, removes no row, sets no class. So not copying them onto our own `<ul>`s costs nothing, which is the opposite of what it looks like
- the Type and Language menus are the same form, `data-autosubmit`, an in-page fetch and a `history.replaceState`. NO `turbo:*` event fires at all — the MutationObserver is the only thing that notices — and they REPLACE the children of `#user-repositories-list`, so a brand-new `<ul>` of brand-new `<li>`s arrives with every `data-sh-*` gone. The rows cannot be kept. What survives is the JS context, so the answer can: a session memo means a dropdown costs zero requests instead of one API call per press
- and the memo has to remember "this repo genuinely has no topics", not only the tagged ones. Keyed on whether a rung ANSWERED (`via` set and not `page-chips`) rather than on whether it found anything — remembering only the tagged left every untagged repo forcing the whole ladder to climb again on the next dropdown, which on a 68-untagged account is the entire cost
- Primer's `.d-inline-block` is `!important`, so COMPACT's `display: flex` on the row's text column lost however specific the selector was. Measured: the row came out 81px instead of 41px and the rule looked simply not to have fired. It is the only `!important` on a `display` in that file and it is there for a stated reason
- a rule shelf lied in four ways at once, and every one of them still LOOKED right. `lang:` was a substring match, so `lang:java` held 26 Java repos and 2 JavaScript ones — a Java shelf of 28, and the same trap waiting in `lang:c` for C++, C#, CSS and Clojure. `name:` matched `owner/name`, so on an account called StarFleet1334 the term `name:star` matched all 54 rows. `private` was listed in CARRIES and never written by `factsFromApi` (the worker had been sending it all along), so `private:false` matched every repository and `private:true` matched none. And `lang:python` on a scraped collection answered "0 matched, 0 unknown" — which reads as "you have no Python" when the truth is "nobody could look". Fixed live and re-measured: the Java shelf went 28 -> 26
- CARRIES claimed the page rung could answer for `language` and `updated`. MEASURED on the server HTML that `scrape()` actually parses — not the hydrated DOM `mark.js` reads — across six real repo pages: **zero** `<relative-time>` and **zero** `a[href*="/search?l="]`. GitHub renders the languages bar and every timestamp on the client now. So `facts.js`'s own header comment, written three hours earlier, saying the two documents "do not differ", is true of the About panel and false of these four fields. A claim about a source is a claim that rots
- `isFork()` returned false for a repo that IS a fork, with the words "forked from" sitting in the HTML: `span.fork-flag` matched nothing and `[data-testid="repo-header"], .Layout-main header` matched nothing. A false here is worse than an absent — `rule.js` treats a carried field as answered, so `fork:false` confidently kept a fork. The finder is structural now: the element must LEAD with the phrase AND link to the repo it was forked from, because the phrase alone is something a README can say
- and the licence link changed shape to `#Apache-2.0-1-ov-file`, which matched neither `a[href*="LICENSE"]` nor `a[href*="#license"]` — so every scraped record came back with no licence at all, and the audit dutifully reported it as a gap. 5 of 6 real repo pages carry the new shape
- the fixture's `updated` is an ISO STRING, because `background.js` does `Date.parse(r.pushed_at)`. A number reaches it as "1756…" and comes back null, so two scenarios were passing a timestamp and testing nothing — the shelf was empty and the weight silent for the right-looking reason
- `[].every()` IS TRUE, so a rule whose every term failed to parse was a UNIVERSAL match — and because the first matching shelf wins, one typo anywhere in `settings.groups` became a black hole. Reproduced: `Oops = topc:ai` drew one shelf holding all three repos and the configured `misc` shelf vanished. The toolbar named the bad term correctly the whole time; naming it is not the same as refusing to build it. It is the exact inversion of the charter's failure shape, which is always "that repo lands in Ungrouped and the page still renders"
- and `.every()` short-circuits, so `unknown` depended on the order the reader typed: `lang:python topic:zzz` reported a repo as unjudgeable and `topic:zzz lang:python` did not, for the same repo and the same rule. A count that changes with word order is not a count. Every term is evaluated now
- the repo-page chip fell to Ungrouped for every rule shelf, because `CARRIES.page` was narrowed on a measurement taken against the SERVER HTML and then applied to the HYDRATED DOM that mark.js reads — where the languages bar and the timestamps are exactly what GitHub's own scripts put there. Same scar as before, running in the opposite direction. The live document is now its own source, `page-live`, and the two entries differ for a stated reason
- two group entries sharing a label built the same `<details>` twice, and the second build re-parented the rows out of the first: a phantom empty shelf with a non-zero count printed on it. Reachable without hand-editing anything, because a suggestion is deduped against the whole entry string, so accepting `ai` while `ai = lang:python` exists collides
- "an honest empty shelf with a stated count beats a confident wrong one" — except an empty shelf was dropped from `order`, so on exactly the collection the rule was built for (scraped, no language, no dates) the page said nothing at all. A rule shelf with unjudged repos is drawn even when it holds none
- `S.seen` was stamped once per `run()`, not once per page — and GitHub's Type/Language filter re-enters `run()`. So "3 since you were here" became nothing the moment the reader touched a dropdown. `run()` is documented idempotent by construction; this was the one piece of state where calling it twice differed from calling it once
- pinned rows were shown in PIN order in the session and re-derived in GitHub's SOURCE order on the next load, so the page quietly rearranged itself: pin `c` then `a` and the session showed `c,a` while the reload showed `a,c`. The store keeps a stamp per pin now, and the harness asserts the two orderings agree rather than trusting a fixture that happened to pin in source order
- a bare word bypassed the `carries` check and searched the full `owner/name` — so `star` matched all 54 rows on an account called StarFleet1334, the scar `name:` had just been fixed for, and it answered a confident `false` about a description nobody had read
- `stars:>= 10` is one term with a space in it: the half that parsed was refused and the orphaned `10` became a silent text search. And `pushed:10d` with no operator is exact float equality against a real timestamp, which can never be true — it only ever "worked" against a fixture built as exactly that. Both are refused and named now
- `pins` was missing from main.js's QUIET list, so every pin press reloaded the page and took the reader's scroll, filter and open shelves with it — the precise cost `overrides` was added to that list to avoid. THE HARNESS COULD NOT SEE IT: `onChanged` was a stub that never fired, so a correct exemption list and a forgotten entry looked identical. jsdom will not let a test stub `location.reload`, but it announces every attempt on the virtual console — `build()` returns a `reloads` counter now, and the scenario asserts both that a pin is quiet AND that an unexempted key still reloads, so the silence means something
- THE SHELF MAP HAD BEEN SILENTLY DEAD SINCE THE PROGRESSIVE RENDER LANDED. `publishMap()` refuses to publish a guess by reading `ctx.provisional`, and `rebucket` cleared the flag on the ELEMENT (`delete host.dataset.provisional`) and not on the context object phase one was called with — so the guard stayed armed for ever. Measured on the live page: `shelfMap` held ZERO owners after a full settled run, on every page with a warm cache, and the repo-page chip therefore had nothing to read. The guard was added in response to a review that correctly warned about publishing a provisional order; making it too broad cost the whole feature and nothing failed
- and the replacement key collided: `shelfmap.write` stamps its own `at` with the time the map was written, so a per-repo `at` map came back as a NUMBER. Renamed `on`. A store that adds a field of its own is a namespace, and writing into it needs to know that
- the repo-page chip re-derived its shelf instead of reading the one the profile recorded, which worked while a shelf was one topic and breaks the moment a shelf is a rule: at the ~160ms the mark is drawn GitHub has not rendered the languages bar or the timestamps — they arrive with its own client-side pass — so `lang:java` was unanswerable and the chip fell to Ungrouped on a repo the profile had on `Java`. Waiting for the DOM to settle is a race with no finish line. The map now carries WHICH SHELF each repo landed on, replacing `names`, which the earlier critique had already found was written and read by nothing — the most sensitive thing in the store, persisted for a feature never built, now replaced by the one the repo page actually needs
